version: '3.9'

services:
  ftp:
    build: .
    container_name: proftpd-server
    ports:
      - "21:21"
      - "30000-30009:30000-30009"
    volumes:
      - D:\Docker\proftpd-uni9:/home/ftpuser
    restart: always